# K-AlphaLab — 국내주식 알고리즘 연구 레포 계획 (Claude Code 핸드오프)

이 문서 하나로 전체 맥락을 파악하고 구현을 시작할 것.
목적: 과거 데이터(차트·수급·공시·뉴스)에서 사람이 가설을 세우면 → Claude가 전략을
코드화 → 한국 시장 규칙이 반영된 백테스트 → 강건성 검증 → 채택/폐기를 기록하는
**로컬 연구 루프**. LLM이 매일 판단하는 게 아니라, LLM이 짠 규칙 코드가 판단한다.

별도 문서 `claude-code-handoff.md`(K-PaperTrade 모의투자 웹)는 이 레포를 통과한
전략의 "순방향 검증" 단계에서만 사용한다. 여기서는 웹·배포·호스팅 DB 없음.
전부 로컬 + 무료.

## 1. 환경
- Python 3.11+ (uv), pandas, pyarrow, duckdb(파케이 쿼리), matplotlib,
  pykrx, FinanceDataReader, OpenDartReader, httpx
- 저장: `data/` 하위 로컬 parquet. 조회는 DuckDB.
- 단 하나의 원격 요소: **GitHub Actions 일일 수집 잡**(평일 공시·뉴스·일봉·수급 →
  repo 커밋). 뉴스·공시는 소급 불가 자산이므로 개발 진도와 무관하게
  **이 잡을 1순위로 가동**한다.

## 2. 연구 5원칙 (위반 시 결과 무효 — README 상단에도 명시)
1. **가설 선행**: 코드로 옮기기 전에 "왜 이게 돈이 되는가" 한 문장(경제적 근거)이
   있어야 한다. 근거를 못 대는 패턴은 구현하지 않는다.
2. **시간 봉인**: 탐색 구간 2019-01-01 ~ 2024-12-31, 봉인 구간 2025-01-01 ~ 현재.
   봉인 데이터는 `data/holdout/`에 물리적으로 분리하고 기본 데이터 로더가 접근을
   차단한다. 해제는 전략 확정 후 전략당 1회, LOG.md에 해제 사실을 기록한다.
3. **비용·제약 포함 백테스트만 유효**: 수수료·거래세·슬리피지·상하한가·상폐를
   뺀 수익률은 보고 금지.
4. **전 시도 기록**: 모든 변형을 LOG.md에 남긴다. "N개 시도 중 최고"는 부풀려진
   숫자임을 항상 병기한다.
5. **순방향 검증 관문**: 봉인 검증까지 통과한 전략만 모의투자(K-PaperTrade →
   키움 모의계좌)로 넘어간다.

## 3. 폴더 구조
```
k-alphalab/
├─ data/
│  ├─ daily/        # 전 종목 일봉 OHLCV + 투자자별 수급 + 시총 (parquet)
│  ├─ minute/       # 일자별 분봉 parquet (네이버 v0 — 매일 적재로 축적)
│  ├─ events/       # 공시·뉴스 이벤트 parquet
│  ├─ meta/         # 상장폐지·관리종목 목록, 거래일 캘린더, 호가단위 표
│  └─ holdout/      # 봉인 구간 (기본 로더 접근 차단)
├─ collector/       # 수집 스크립트 + .github/workflows 일일 잡
├─ backtest/
│  ├─ engine.py     # 체결·비용 규칙 (아래 5장)
│  ├─ event_study.py# 이벤트 정렬 수익률(CAR) 분석
│  ├─ metrics.py    # 수익률·MDD·샤프·승률·연도별 분해
│  └─ robustness.py # 파라미터 스윕, 기간·종목군 분해
├─ strategies/      # 1파일 = 1전략 (아래 6장 규격)
├─ reports/         # 전략별 자동 리포트 (md + png)
├─ LOG.md           # 전 시도 기록 (append-only)
└─ README.md
```

## 4. 데이터 레이어
### 4.1 시세·수급
- pykrx: 전 종목 일봉, 투자자별(외국인/기관/개인) 순매수, 시가총액 — 2019년부터 백필.
- FinanceDataReader: 종목 리스팅, 지수(KS11·KQ11), KRX-DELISTING(상장폐지),
  KRX-ADMINISTRATIVE(관리종목) — 생존편향 제거용.
- 분봉: 네이버 비공식 엔드포인트(요청 간격 초당 1회 이하, 지수 백오프, 방어적
  파싱). 최근분만 제공되므로 매일 적재로 히스토리를 직접 쌓는다. v2에서
  KiwoomRestProvider로 교체 가능하게 provider 인터페이스 유지.

### 4.2 이벤트 (공시·뉴스)
- 1순위 **DART 공시**(OpenDartReader): 접수 분 단위 시각 + 유형 코드. 우선 수집
  유형 — 유상증자, 전환사채(CB), 최대주주 변경, 단일판매·공급계약, 감사의견,
  소송, 자기주식.
- 2순위 **네이버 금융 종목별 뉴스** 크롤: 과거 헤드라인 백필이 가능한 사실상
  유일한 무료 소스. 헤드라인 + 날짜(시각 있으면 함께).
- 3순위(지속 수집): 네이버 뉴스 검색 API(공식, 백필은 얕음), 대형 이슈 소급은
  빅카인즈.
- `events` 스키마: `(ticker, ts, ts_precision 'minute'|'date', event_type,
  source, headline, url)` + 배치가 `ret_d1/ret_d5/ret_d20`을 채운다.
- **룩어헤드 규칙(엔진에 하드코딩)**: ts_precision='minute' → 해당 시각 이후
  첫 체결부터 진입 가능. 'date' → **다음 거래일 시가**부터. "당일 종가 진입" 금지.
- 이벤트 텍스트의 1차 라벨은 규칙 기반(공시 유형 코드·키워드)으로 무료 처리.
  LLM 감성분석은 살아남은 가설의 정밀 검증 단계에서 헤드라인만·배치로 최소 사용
  (유일한 Claude API 비용 지점 — 사전 승인 후 실행).

## 5. 백테스트 엔진 규칙 (K-PaperTrade와 동일 규칙, 일봉 벡터화 우선)
1. 체결: 신호 발생 **다음 거래일 시가** + 슬리피지 bp(기본 5, config).
2. 가격제한폭 ±30%: 상한가 마감일 매수 체결 불가, 하한가 마감일 매도 불가.
   참고 구현: HKUDS/Vibe-Trading의 KRX 백테스트 엔진(체결 시점 판정).
3. 비용: 수수료 0.015%(config) + 매도 거래세(config — 세율 변동 대비 설정값,
   구현 시점의 현재 세율을 확인해 기본값 기입).
4. 거래정지·거래량 0 체결 불가. 상장폐지·관리종목 반영(당시 시점 기준).
5. 호가단위 반올림(지정가 시뮬 시).
6. 롱온리 기본(개인 공매도 제약), 현금·포지션 회계, 동시 보유 종목 상한 config.
7. 분봉 백테스트는 v2 확장 — 엔진 인터페이스만 호환되게 설계.
- 규칙별 단위테스트(pytest) 필수: 상한가 거부 / 비용 계산 / 룩어헤드 / 상폐 처리.

## 6. 전략 파일 규격 (strategies/)
- 1파일 = 1전략. 상단 docstring 필수 항목: 가설 한 문장 / 경제적 근거 /
  사용 데이터 / 파라미터 목록 / 상태(탐색중·폐기·확정·봉인검증완료).
- `generate_signals(data, params) -> DataFrame[ticker, date, action]` 순수 함수.
  함수 시그니처상 미래 데이터에 접근 불가능하게 설계.

## 7. 리포트와 강건성 (reports/ 자동 생성)
- 기본 지표: 누적수익률 vs KOSPI·KOSDAQ, 연도별 분해, MDD, 샤프, 승률,
  평균 보유일, 거래횟수, **비용 차감 전/후 비교**.
- 채택 조건(robustness.py가 자동 판정):
  - 파라미터 ±20% 스윕에서 수익 부호 유지
  - 연도별 분해에서 3개 구간 이상 흑자
  - 시총 상·하위 분해 결과 병기(한쪽 몰림 여부 표시)
  - 거래횟수 30건 미만이면 "표본 부족" 판정
- 이벤트 전략은 event_study: 이벤트 정렬 평균 CAR 곡선(D-5 ~ D+20) + 표본수.

## 8. LOG.md 규격 (append-only)
`| # | 날짜 | 가설 | 전략파일 | 탐색구간 성과 | 강건성 | 판정 | 배운 것 |`
- 매 시도 종료 시 1행 추가. 폐기도 기록한다 — 이 레포의 절반의 가치는
  "안 되는 것의 목록"이다.

## 9. Claude의 행동 규칙 (이 레포 안에서)
- 가설을 코드로 옮길 때 임의 조건 추가 금지. 개선 아이디어는 별도 시도로 제안.
- 모든 백테스트 후 robustness를 자동 실행하고, 결과 요약에 과적합 위험 평가
  1줄을 반드시 포함("이번이 몇 번째 변형인지" 명시).
- 봉인 구간(`data/holdout/`) 접근이 감지되면 작업 중단 후 경고.
- 성과가 좋아도 "과거 성과는 미래를 보장하지 않음 · 연구·교육용" 문구를
  모든 리포트에 포함.

## 10. 개발 순서 (커밋 단위)
1. 스캐폴딩 + 일봉·수급 백필(2019~2024) + 거래일 캘린더 + holdout 물리 분리
2. 이벤트 수집기(DART → 네이버 종목뉴스) + **GitHub Actions 일일 잡 즉시 가동**
3. backtest/engine.py + 규칙 단위테스트
4. event_study·metrics·robustness + 리포트 템플릿
5. 파이프라인 검증용 예제 2개: (a) 20일 모멘텀 벤치마크(기준선),
   (b) 아래 첫 가설 중 1개
6. LOG.md 운영 시작

## 11. 첫 가설 후보 (5단계에서 하나 선택)
- H1: 유상증자(주주배정/3자배정 구분) 공시 후 D+1~D+20 드리프트
- H2: 외국인+기관 동반 순매수 3일 이상 지속 종목의 이후 5·20일 수익
- H3: 상한가 마감 다음날의 시가 갭과 당일 수익의 관계

## 12. 금지
- 봉인 구간 조회·시각화 등 일체 접근(확정 검증 1회 제외)
- 비용 제외 수익률 보고, 실거래 연동, 유료 API, 네이버 과호출
- 수익 보장·추천성 표현

지금 1단계부터 시작해줘. 먼저 폴더 구조와 설정 파일 계획을 보여준 뒤 진행해.