[project]
name = "collector"
version = "0.1.0"
description = "KRX 시세 수집 배치 — 네이버 분봉(v0) / pykrx / FinanceDataReader"
requires-python = ">=3.11"
dependencies = [
    "finance-datareader>=0.9.202",
    "httpx>=0.28.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.0",
    "pykrx>=1.0.51",
    "setuptools<81",
]
# 2단계에서 uv add: httpx pandas pyarrow pykrx finance-datareader
