# Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env
.env
.env.local
.env*.local

# Python
__pycache__/
.venv/
*.pyc

# 수집 데이터 (parquet는 GitHub Release로만 배포)
data/
.data/ # 로컬 SQLite 폴백(krx_market.db·trading.db) — 계좌·주문 데이터가 커밋되면 안 된다
*.parquet
.gstack/

# Playwright
test-results/
playwright-report/

# Vercel (.env* 규칙은 위 env 섹션에 있음 — .env.example은 추적해야 하므로 추가 금지)
.vercel
